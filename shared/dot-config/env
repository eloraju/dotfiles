#!/bin/sh
export LOCAL_BIN=$HOME/.local/bin
export USER_BIN=$HOME/.local/user/bin
export SCRIPTS_DIR=$HOME/.local/user/scripts
LOCAL_BINS=$LOCAL_BIN:$USER_BIN:$SCRIPTS_DIR

# XDG DIRS
export XDG_CONFIG_HOME=$HOME/.config
export XDG_CACHE_HOME=$HOME/.local/cache
export XDG_DATA_HOME=$HOME/.local/share

# Rust packages
export CARGO_INSTALL_ROOT=$XDG_DATA_HOME/cargo
CARGO_BIN=$CARGO_INSTALL_ROOT/bin
RUSTPUP_BIN=$HOME/.cargo/bin
RUST_BINS=$CARGO_BIN:$RUSTPUP_BIN

# zsh
export ZDOTDIR=$HOME/.config/zsh

# GnuPG
export GNUPGHOME=$XDG_DATA_HOME/gnupg

# nvim as editor
export VISUAL=nvim
export EDITOR=$VISUAL
export SUDO_EDITOR=$VISUAL

export KEYTIMEOUT=1

# History stuff
export HISTFILE="$HOME/.local/share/zsh/history"
export HISTSIZE=10000
export SAVEHIST=$HISTSIZE
#export MANPAGER="sh -c 'col -bx |bat -l man -p'"
export MANPAGER="nvim +Man!"

export NVM_DIR="$HOME/.nvm"

# Android studio stuff
export ANDROID_HOME="/Users/juuso.elo-rauta/Library/Android/sdk"

# Add android home to path
#ANDROID_EMULATOR="$ANDROID_HOME/emulator"
#ANDROID_PLAT_TOOLS="$ANDROID_HOME/platform-tools"
#ANDROID_BINS=$ANDROID_HOME:$ANDROID_EMULATOR:$ANDROID_PLAT_TOOLS

# Add intellij idea to path
IDEA_BIN=/Applications/IntelliJ\ IDEA.app/Contents/MacOS

# add homebrew to path before other stuff to prevent duplication
eval "$(/opt/homebrew/bin/brew shellenv)"

export PATH=$LOCAL_BINS:$RUST_BINS:$ANDROID_BINS:$IDEA_BIN:$PATH
